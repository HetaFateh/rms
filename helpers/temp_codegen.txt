Insight about this flow:
- after implementing it, run the test ONLY FOR THIS TEST CASE (disable all test case except this)



Create new test case for this use case:
A. create kategori program flow:
    1. login as admin
    2. await page.getByRole('link', { name: 'Kategori Program' }).click();
    3. await page.getByRole('button', { name: 'Tambah' }).click();
    4. await page.locator('input[name="NAMA"]').click();
    5. await page.locator('input[name="NAMA"]').fill('automation kategori program');
    6. await page.locator('input[name="KODE"]').click();
    7. await page.locator('input[name="KODE"]').fill('atcode');
    8. await page.locator('input[name="DESKRIPSI"]').click();
    9. await page.locator('input[name="DESKRIPSI"]').fill('automation deskripsi');
    10. await page.getByRole('button', { name: 'Simpan' }).click();
    11. verify success popup
  
B. edit kategori program flow:
    1. await page.getByRole('textbox', { name: 'Search' }).click();
    2. await page.getByRole('button').filter({ hasText: /^$/ }).nth(2).click();
    3. edit the data (only add simple character), this is each of the locator ==await page.locator('#nama').click();
                                                                                await page.locator('#code').click();
                                                                                await page.locator('#desc').click();
    4. submit the data
    5. verify success popup

C. delete kategori program flow:
    1. await page.getByRole('textbox', { name: 'Search' }).click();
    2. await page.getByRole('button').filter({ hasText: /^$/ }).nth(3).click();
    3. await page.getByRole('button', { name: 'Hapus' }).click();
    4. verify success popup
