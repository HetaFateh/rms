Read this instruction first:
- After created new user as Agent, save that email as temporary variable and use it to verify account as Agent.
- The create agent test case is connected to verify account by agent test case, so it's a e2e test case but created it as 2 test case (the flow is create agent, then logout, then verify account by that agent)
- 

Verify Account by Agent Flow:
1. login using Agent temporary email that just been created, the password is always "password"
2. 